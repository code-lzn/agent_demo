spring:
  application:
    name: Agent_demo
  profiles:
    active: dev

server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.limou.agent_demo.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn